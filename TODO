cleanup syscall.c

syscalls for specifying labels

object persistence
will probably need an iovec interface to disk_io()

simple file system
user-level elf loader
spawn

a more interesting shell

===

labels for address spaces?
    we can stick a label in (struct Page)

what happens to pages mapped RW (or for that matter any pages) when th_label changes?
    make any th_label changes happen by jumping through a gate?

